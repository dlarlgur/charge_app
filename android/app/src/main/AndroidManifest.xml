<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <application
        android:label="전기차 기름차"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 네이버 지도 SDK -->
        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="${naverMapClientId}" />
        <!-- flutter_local_notifications: 앱 열지 않고 알림 액션 처리 (읽음 버튼 등) -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.dexterous.flutterlocalnotifications.ACTION_TAPPED" />
            </intent-filter>
        </receiver>

        <!-- 주유소 홈 위젯 (D - Medium 4x2) -->
        <receiver
            android:name=".GasWidgetProvider"
            android:label="@string/gas_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gas_widget_info" />
        </receiver>

        <!-- 충전소 홈 위젯 (E - Medium 4x2) -->
        <receiver
            android:name=".EvWidgetProvider"
            android:label="@string/ev_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ev_widget_info" />
        </receiver>

        <!-- 전기차 기름차 통합 (Large 4x4) -->
        <receiver
            android:name=".CombinedWidgetProvider"
            android:label="@string/combined_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/combined_widget_info" />
        </receiver>

        <!-- 주유 최저가 (Small 2x2) -->
        <receiver
            android:name=".GasSmallWidgetProvider"
            android:label="@string/gas_small_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gas_small_widget_info" />
        </receiver>

        <!-- 충전 가용 (Small 2x2) -->
        <receiver
            android:name=".EvSmallWidgetProvider"
            android:label="@string/ev_small_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ev_small_widget_info" />
        </receiver>

        <!-- home_widget 위젯 버튼(새로고침) → 백그라운드 Dart 콜백 수신 -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>
        <service
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Android Auto 선언 제거 — 실제 Auto 앱(미디어/메시징/내비)이 아니라
             Google Play 자동차 앱 품질 검수에서 거부됨(버전코드 10). 차량용 미통합. -->

        <!-- AdMob App ID (charge — 전기차 기름차) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8640148276009977~8762972754" />

        <!-- 카카오 로그인 redirect (scheme = kakao{네이티브앱키}) -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="kakao88122d611d07cc854eebf5b50d0920f4"
                    android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- 네이버 로그인 (flutter_naver_login) — clientId/secret/name -->
        <meta-data android:name="com.naver.sdk.clientId" android:value="F8KFHjLPiaLBTrOrfk3N" />
        <meta-data android:name="com.naver.sdk.clientSecret" android:value="Y3CopbAxAF" />
        <meta-data android:name="com.naver.sdk.clientName" android:value="전기차 기름차" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 네이버 지도 길찾기 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nmap" />
        </intent>
        <!-- 카카오 내비 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="kakaonavi" />
        </intent>
        <!-- 티맵 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tmap" />
        </intent>
        <!-- 전화 -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <!-- 플레이스토어 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
        <!-- https (url_launcher) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
